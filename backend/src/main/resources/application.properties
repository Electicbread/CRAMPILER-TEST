# --- Data Tier: MongoDB Atlas ---
# Replace the line below with your real Atlas connection string
# (Atlas dashboard -> Database -> Connect -> Drivers), including your
# database username/password from Database Access (not your Atlas login).
# Spring Boot only reads this file at startup, so restart the app after
# editing it.
#
# Example:
# spring.data.mongodb.uri=mongodb+srv://myuser:mypassword@cluster0.abcde.mongodb.net/crampiler?retryWrites=true&w=majority
spring.data.mongodb.uri=mongodb+srv://rascoalfred04_db_user:siomaisiopaosuman@crampiler.jp85y1c.mongodb.net/crampilerV2?appName=CramPiler

# --- External Service: Blackboard Learn LMS ---
# Optional: set this so the scheduled hourly poller can run unattended.
# You can also leave this blank and only sync manually via the app's Sync tab.
crampiler.blackboard.ics-url=

# --- Scheduled Poller cadence (ms). Default: 1 hour, per the architecture doc's 1-2hr window. ---
crampiler.sync.interval-ms=3600000

# --- Server ---
server.port=8080

# --- CORS target: the Vite dev server ---
crampiler.client.origin=http://localhost:5173

# --- AI Chat Assistant (Groq — free tier, no credit card required) ---
# Get a free key at console.groq.com -> API Keys -> Create API Key.
# Never put the real key on this line — set CRAMPILER_CHAT_API_KEY as an
# environment variable instead (same lesson as the Atlas credential).
crampiler.chat.api-key=${CRAMPILER_CHAT_API_KEY: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}
crampiler.chat.api-url=https://api.groq.com/openai/v1/chat/completions
crampiler.chat.model=openai/gpt-oss-20b
